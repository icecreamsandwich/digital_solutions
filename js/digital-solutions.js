/**
 * @file
 * digital_solutions behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.digitalSolutions = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
